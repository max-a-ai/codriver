# Perception

Start the pipelines top to bottom: preprocessing, then detection, then pose
inference.

Pose inference stays orange while it loads its checkpoint, which takes most of a
minute. That is normal. Red means it started and died, and the log button says
why.

TODO: paste in the checkpoint paths and the expected startup output.
