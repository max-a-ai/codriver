# instructions

One note per tab, opened by the TXT button in the panel's right-hand rail. The
filename is the tab id, so `status.md` is what the Status row opens.

Keep them short. This is what somebody reads standing next to the car, not
documentation. A missing file is fine and shows as "no notes yet".

Several of these are placeholders until the equivalents from the car computer
are copied in.
